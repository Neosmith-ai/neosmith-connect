title: NeoSmith Developer Guide
description: Connect your IDE and AI coding agent to NeoSmith in under 5 minutes.
remote_theme: just-the-docs/just-the-docs

# Pages build: now driven by .github/workflows/pages.yml on push to main.
# Repo Settings → Pages → "Build source" must be flipped to "GitHub Actions"
# (one-time setup). Until flipped, builds succeed locally and Pages still
# serves /docs/ from the dangling repo setting.

color_scheme: light
search_enabled: true

# Pretty URLs (no .html) so in-content cross-links like
# ../agents/claude-code resolve correctly.
permalink: pretty

# Header / external links
aux_links_new_tab: true

nav_external_links:
  - title: contact-us@neosmith.ai
    url: mailto:contact-us@neosmith.ai

# Exclude source-of-truth duplicates / internal files from the build.
# vendor/ must be excluded or Jekyll tries to parse gem internals as posts.
exclude:
  - CONTRIBUTING.md
  - "*.local.md"
  - vendor/
  - Gemfile
  - Gemfile.lock

plugins:
  - jekyll-remote-theme
  - jekyll-include-cache
