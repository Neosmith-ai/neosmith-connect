# Node / npm
node_modules/
npm-debug.log
yarn-error.log

# Sandbox artifacts produced by the contract test suite
packages/cli/.sandbox/

# Scaffolder bookkeeping (re-stamped per scaffold run)
SCAFFOLD_SOURCES.json

# Jekyll build artifact
_site/

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
.history/

# Per-OS quirks
*.swp

# Stray test-config dirs left behind by a run of `neosmith <harness> on`
# against a real machine — never meant to be checked into the repo.
.continue/
.codex/

# Personal scratch space — one-shot validation scripts and other tools that
# lived alongside the repo for an investigation. Not part of the project.
scripts/_one_shot/
